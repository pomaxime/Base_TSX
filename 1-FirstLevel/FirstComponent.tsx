export default function Hero() {
  return (
    <section>
      <h1>Bienvenue</h1>
      <p>Mon site stylé</p>
    </section>
  )
}

// function Hero() → composant
// return (...) → JSX (HTML dans JS)
// function Hero() → composant
// return (...) → JSX (HTML dans JS)
